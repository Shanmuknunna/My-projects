runs_per_over = int(input("What is the run rate per over :"))

total_overs = int(input("What is the total overs of the match :"))

total_runs = (runs_per_over * total_overs)
print("Total runs of the match: ", total_runs)
print("Thank you")